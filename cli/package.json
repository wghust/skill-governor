{
  "name": "skill-governor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "skill-governor": "./dist/bin.js"
  },
  "scripts": {
    "build": "npm run build:cli && npm run build:runtime",
    "build:cli": "tsc -p tsconfig.json",
    "build:runtime": "node ./scripts/build-runtime.mjs",
    "test": "vitest run",
    "test:build": "npm run build"
  },
  "dependencies": {
    "commander": "^14.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
